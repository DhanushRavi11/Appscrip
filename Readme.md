# Hello World – Kubernetes + ArgoCD Demo

This repository demonstrates a simple end-to-end setup using Docker, Kubernetes, ArgoCD, and a one-click deployment script.

## Components

- Simple web app that returns `Hello, world!`.
- Dockerfile to build an image for the app.
- Kubernetes manifests (Namespace, Deployment, Service).
- ArgoCD `Application` resource for GitOps-style deployment.
- `deploy.sh` script to automate build, push, and deploy.

---
